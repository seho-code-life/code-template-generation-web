import request from "@/common/request";

<%# 开发者选择了create %> 
<% if(checkFeatures.includes("create")) {%>
/**
 * @name 新增<%= moduleCN _%> 数据
 * @params {Object} params - 需要新增的数据结构体
 * @description  This code is completed by the generator (create)
 * @returns Data
 */

export async function create<%= moduleName.capital _%> Data(params){
  return new Promise((resolve) => {
    request({
      method: "POST",
      url: "<%= url _%>",
      data: params
    }).then((res) => {
      resolve(res);
    });
  });
}
<% } %>
<%# 开发者选择了read %> 
<% if(checkFeatures.includes("read")) {%>

/**
 * @name 获取<%= moduleCN _%> 列表数据
 * @description  This code is completed by the generator (read)
 * @returns List<Data>
 */
export async function get<%= moduleName.capital _%> List(){
  return new Promise((resolve) => {
    request({
      method: "GET",
      url: "<%= url _%>",
    }).then((res) => {
      resolve(res);
    });
  });
}

/**
 * @name 获取<%= moduleCN _%> 指定资源
 * @params {Object} params - 获取指定资源API的参数
 * @params {string} params.id - 资源ID
 * @description  This code is completed by the generator (read)
 * @returns Data
 */

 export async function get<%= moduleName.capital _%> ByID(params){
  return new Promise((resolve) => {
    request({
      method: "GET",
      url: `<%= url_%>/${params.id}`,
    }).then((res) => {
      resolve(res);
    });
  });
}
<% } %>


<%# 开发者选择了update %> 
<% if(checkFeatures.includes("update")) {%>

/**
 * @name 修改<%= moduleCN _%> 指定资源
 * @params {Object} params - 修改指定资源API的参数
 * @params {string} params.id - 资源ID
 * @description  This code is completed by the generator (update)
 * @returns Data
 */

 export async function update<%= moduleName.capital _%> ByID(params){
  return new Promise((resolve) => {
    request({
      method: "PATCH",
      url: `<%= url _%>/${params.id}`,
    }).then((res) => {
      resolve(res);
    });
  });
}
 <% } %>

<%# 开发者选择了delete %> 
<% if(checkFeatures.includes("delete")) {%>
/**
 * @name 删除<%= moduleCN _%> 指定资源
 * @params {Object} params - 删除指定资源API的参数
 * @params {string} params.id - 资源ID
 * @description  This code is completed by the generator (delete)
 * @returns Data
 */
 
export async function delete<%= moduleName.capital _%> ByID(params){
  return new Promise((resolve) => {
    request({
      method: "DELETE",
      url: `<%= url _%>/${params.id}`,
    }).then((res) => {
      resolve(res);
    });
  });
}
<% } %>
